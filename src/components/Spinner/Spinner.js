import React from 'react';
import "./Spinner.css"

const Spinner = () => {
    return (
        <div className="pos-center">
            <div className="loader"/>
        </div>
    );
};

export default Spinner;